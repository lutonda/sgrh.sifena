{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Documento list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Tipodocumento</th>
                <th>Ndi</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('documento_show', { 'id': entity.id }) }}">{{ entity.tipoDocumento }}</a></td>
                <td>{{ entity.ndi }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('documento_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('documento_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('documento_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
