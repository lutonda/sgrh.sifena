{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Formacao list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Curso</th>
                <th>Descricao</th>
                <th>Grauacademico</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('formacao_show', { 'id': entity.id }) }}">{{ entity.curso }}</a></td>
                <td>{{ entity.descricao }}</td>
                <td>{{ entity.grauAcademico }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('formacao_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('formacao_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('formacao_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
