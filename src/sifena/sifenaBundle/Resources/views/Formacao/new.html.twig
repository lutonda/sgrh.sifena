{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Formacao creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('formacao') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
